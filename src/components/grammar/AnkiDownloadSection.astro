---
// Component: AnkiDownloadSection
// Description: Download section specifically for grammar-related Anki decks
// Used in: grammar pages
// Purpose: Provides download links for related Anki decks for each grammar pattern

export interface Props {
  grammarPattern: string;
  relatedAnkiDecks?: {
    title: string;
    downloadUrl: string;
    fileSize?: string;
    description?: string;
    level?: string;
  }[];
}

const { grammarPattern, relatedAnkiDecks = [] } = Astro.props;

// Function to find related Anki decks based on grammar pattern
function findRelatedAnkiDecks(pattern: string) {
  // Map common grammar patterns to their related Anki decks
  const grammarToAnkiMap = {
    '-은 것1': [
      {
        title: 'Tiếng Hàn Tổng Hợp 1 - Bài 1',
        downloadUrl: '/downloads/anki-tieng-han-tong-hop-1-bai-1.apkg',
        fileSize: '2.1 MB',
        description: 'Từ vựng và câu ví dụ sử dụng -은 것',
        level: 'Beginner'
      }
    ],
    '-은 것2': [
      {
        title: 'Tiếng Hàn Tổng Hợp 1 - Bài 2',
        downloadUrl: '/downloads/anki-tieng-han-tong-hop-1-bai-2.apkg',
        fileSize: '1.8 MB',
        description: 'Tính từ với -은 것 và ví dụ thực tế',
        level: 'Beginner'
      }
    ],
    '-고': [
      {
        title: 'Ngữ pháp cơ bản - Liên từ',
        downloadUrl: '/downloads/anki-basic-conjunctions.apkg',
        fileSize: '1.5 MB',
        description: 'Các liên từ cơ bản bao gồm -고',
        level: 'Beginner'
      }
    ],
    '-어서/-아서': [
      {
        title: 'Nguyên nhân và kết quả',
        downloadUrl: '/downloads/anki-cause-effect.apkg',
        fileSize: '2.3 MB',
        description: 'Ngữ pháp diễn tả nguyên nhân',
        level: 'Beginner'
      }
    ],
    '-니까': [
      {
        title: 'Ngữ pháp lý do chủ quan',
        downloadUrl: '/downloads/anki-subjective-reasons.apkg',
        fileSize: '1.9 MB',
        description: 'Các cấu trúc diễn tả lý do',
        level: 'Beginner'
      }
    ],
    '-지만': [
      {
        title: 'Liên từ đối lập',
        downloadUrl: '/downloads/anki-contrast-grammar.apkg',
        fileSize: '1.7 MB',
        description: 'Ngữ pháp diễn tả sự đối lập',
        level: 'Beginner'
      }
    ],
    '-면': [
      {
        title: 'Ngữ pháp điều kiện',
        downloadUrl: '/downloads/anki-conditional.apkg',
        fileSize: '2.0 MB',
        description: 'Các cấu trúc điều kiện trong tiếng Hàn',
        level: 'Beginner'
      }
    ]
  };

  return grammarToAnkiMap[pattern] || [];
}

// Get related decks (from props or auto-find)
const ankiDecks = relatedAnkiDecks.length > 0 ? relatedAnkiDecks : findRelatedAnkiDecks(grammarPattern);
---

{ankiDecks.length > 0 && (
  <section id="anki-download-section" class="anki-download-section">
    <h2>🎴 Anki Deck liên quan</h2>
    <p class="section-description">
      Thẻ Anki được tuyển chọn giúp bạn luyện tập ngữ pháp <strong>{grammarPattern}</strong> hiệu quả
    </p>
    
    <div class="anki-grid">
      {ankiDecks.map(deck => (
        <a href={deck.downloadUrl} target="_blank" class="anki-card">
          <div class="anki-icon">🎴</div>
          <div class="anki-info">
            <h3>{deck.title}</h3>
            <p>{deck.description || 'Thẻ Anki cho ngữ pháp này'}</p>
            <div class="anki-meta">
              {deck.level && (
                <span class={`level-badge ${deck.level.toLowerCase()}`}>
                  {deck.level}
                </span>
              )}
              {deck.fileSize && (
                <span class="file-size">{deck.fileSize}</span>
              )}
            </div>
          </div>
          <div class="download-indicator">⬇️</div>
        </a>
      ))}
    </div>
    
    <div class="anki-note">
      <div class="note-content">
        <div class="note-icon">💡</div>
        <div class="note-text">
          <p><strong>Cách sử dụng:</strong></p>
          <ol>
            <li>Tải file .apkg về máy</li>
            <li>Mở ứng dụng Anki và import file</li>
            <li>Học theo lịch trình mà Anki đề xuất</li>
            <li>Kết hợp với bài học ngữ pháp để hiệu quả tối đa</li>
          </ol>
        </div>
      </div>
      
      <div class="download-anki-app">
        <p><strong>Chưa có Anki?</strong></p>
        <div class="anki-links">
          <a href="https://apps.ankiweb.net/" target="_blank" class="anki-download-link">
            💻 Tải Anki Desktop
          </a>
          <a href="https://play.google.com/store/apps/details?id=com.ichi2.anki" target="_blank" class="anki-download-link">
            📱 Tải Anki Android
          </a>
          <a href="https://apps.apple.com/app/ankimobile-flashcards/id373493387" target="_blank" class="anki-download-link">
            🍎 Tải AnkiMobile iOS
          </a>
        </div>
      </div>
    </div>
  </section>
)}

<style>
  .anki-download-section {
    background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
    color: white;
    padding: 2rem;
    border-radius: 16px;
    margin: 2rem 0;
    scroll-margin-top: 100px;
  }

  .anki-download-section h2 {
    text-align: center;
    margin-bottom: 1rem;
    font-size: 1.8rem;
    text-shadow: 0 2px 4px rgba(0,0,0,0.2);
  }

  .section-description {
    text-align: center;
    margin-bottom: 2rem;
    font-size: 1.1rem;
    opacity: 0.95;
  }

  .anki-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .anki-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 12px;
    padding: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    text-decoration: none;
    color: white;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .anki-card:hover {
    transform: translateY(-2px);
    background: rgba(255, 255, 255, 0.25);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }

  .anki-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.5s;
  }

  .anki-card:hover::before {
    left: 100%;
  }

  .anki-icon {
    font-size: 3rem;
    flex-shrink: 0;
  }

  .anki-info {
    flex: 1;
  }

  .anki-info h3 {
    margin: 0 0 0.5rem 0;
    font-size: 1.2rem;
    font-weight: 600;
  }

  .anki-info p {
    margin: 0 0 0.75rem 0;
    opacity: 0.9;
    line-height: 1.4;
  }

  .anki-meta {
    display: flex;
    gap: 0.75rem;
    align-items: center;
  }

  .level-badge {
    padding: 0.25rem 0.5rem;
    border-radius: 12px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
  }

  .level-badge.beginner {
    background: rgba(34, 197, 94, 0.8);
  }

  .level-badge.intermediate {
    background: rgba(251, 191, 36, 0.8);
  }

  .level-badge.advanced {
    background: rgba(239, 68, 68, 0.8);
  }

  .file-size {
    background: rgba(255, 255, 255, 0.2);
    padding: 0.25rem 0.5rem;
    border-radius: 8px;
    font-size: 0.75rem;
    font-weight: 500;
  }

  .download-indicator {
    font-size: 1.5rem;
    opacity: 0.8;
    transition: transform 0.3s ease;
  }

  .anki-card:hover .download-indicator {
    transform: translateY(2px);
  }

  .anki-note {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 1.5rem;
    backdrop-filter: blur(5px);
  }

  .note-content {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .note-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .note-text ol {
    margin: 0.5rem 0 0 0;
    padding-left: 1.25rem;
  }

  .note-text li {
    margin-bottom: 0.25rem;
    line-height: 1.4;
  }

  .download-anki-app {
    border-top: 1px solid rgba(255, 255, 255, 0.2);
    padding-top: 1.5rem;
  }

  .download-anki-app p {
    margin-bottom: 1rem;
    font-weight: 600;
  }

  .anki-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .anki-download-link {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    text-decoration: none;
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.3s ease;
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  .anki-download-link:hover {
    background: rgba(255, 255, 255, 0.3);
    transform: translateY(-1px);
  }

  /* Responsive design */
  @media (max-width: 768px) {
    .anki-download-section {
      padding: 1.5rem;
    }

    .anki-grid {
      grid-template-columns: 1fr;
    }

    .anki-card {
      padding: 1.25rem;
    }

    .note-content {
      flex-direction: column;
      gap: 0.5rem;
    }

    .anki-links {
      flex-direction: column;
    }

    .anki-download-link {
      text-align: center;
    }
  }
</style>
