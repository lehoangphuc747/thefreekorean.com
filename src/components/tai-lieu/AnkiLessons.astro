---
// --- Props Interface ---
export interface Props {
  lessons: Array<{
    number: number;
    title: string;
    cards: number;
    fileSize: string;
    downloadUrl: string;
    date: string;
  }>;
}

// --- Component Logic ---
const { lessons } = Astro.props;
---

<!-- --- Template --- -->
<div class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden">
  <!-- Header compact -->
  <div class="bg-gradient-to-r from-blue-600 to-purple-600 px-6 py-4">
    <h2 class="text-xl font-bold text-white">📚 Danh sách bài học</h2>
  </div>
  
  <!-- Content tối ưu không gian -->
  <div class="p-4">
    <!-- Grid responsive với nhiều cột hơn -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
      {lessons.map((lesson) => (
        <div class="group relative bg-white border border-gray-200 rounded-lg p-4 hover:shadow-lg hover:border-blue-300 transition-all duration-200">
          
          <!-- Header compact -->
          <div class="mb-3">
            <h3 class="text-lg font-bold text-gray-900 mb-2 group-hover:text-blue-600 transition-colors duration-200 text-center">
              {lesson.title}
            </h3>
            
            <!-- Stats inline compact -->
            <div class="space-y-2">
              <div class="flex items-center justify-between text-sm">
                <div class="flex items-center space-x-1">
                  <div class="w-4 h-4 bg-green-100 rounded-full flex items-center justify-center">
                    <svg class="w-2.5 h-2.5 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                  </div>
                  <span class="text-gray-700">{lesson.cards} thẻ</span>
                </div>
                <div class="flex items-center space-x-1">
                  <div class="w-4 h-4 bg-blue-100 rounded-full flex items-center justify-center">
                    <svg class="w-2.5 h-2.5 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd"/>
                    </svg>
                  </div>
                  <span class="text-gray-700">{lesson.fileSize}</span>
                </div>
              </div>
            </div>
          </div>
          
          <!-- Nút download compact -->
          <a 
            href={lesson.downloadUrl}
            target="_blank"
            class="group/btn w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-medium py-2 px-4 rounded-lg text-center transition-all duration-200 flex items-center justify-center space-x-2"
          >
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
            </svg>
            <span>Tải về</span>
          </a>
          
        </div>
      ))}
    </div>
    
  </div>
</div>
