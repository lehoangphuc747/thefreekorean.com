---
// Dynamic route for documents
// Currently disabled - individual document pages are handled by static files
export async function getStaticPaths() {
  // Return empty array to disable dynamic routing for now
  // Individual document pages are served from static .astro files in subdirectories
  return [];
}

// This component will not be reached due to empty getStaticPaths
// Document content is served from static files
---

<div>
  <p>Document content is served from static files.</p>
</div>