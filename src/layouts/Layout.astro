---
import NavBar from '../components/NavBar.astro';
import Footer from '../components/Footer.astro';
import ScrollToTop from '../components/ScrollToTop.astro';
import '../styles/global.css';

export interface Props {
	title?: string;
	description?: string;
	keywords?: string[];
	image?: string;
	url?: string;
	type?: string;
	lang?: string;
}

const {
	title = 'The Free Korean - Tài liệu học tiếng Hàn miễn phí',
	description = 'Tài liệu học tiếng Hàn miễn phí: ngữ pháp, từ vựng, đề thi TOPIK, giáo trình, Anki flashcards. Học tiếng Hàn hiệu quả cùng The Free Korean.',
	keywords = ['học tiếng Hàn', 'tài liệu tiếng Hàn', 'ngữ pháp tiếng Hàn', 'TOPIK', 'từ vựng tiếng Hàn'],
	image = '/images/og-default.jpg',
	url = Astro.url.href,
	type = 'website',
	lang = 'vi'
} = Astro.props as Props;

const siteUrl = Astro.site?.href || 'https://thefreekorean.com';
const fullImageUrl = image.startsWith('http') ? image : `${siteUrl}${image}`;
const fullUrl = url.startsWith('http') ? url : `${siteUrl}${url}`;
---
<!doctype html>
<html lang={lang}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		
		<!-- Primary Meta Tags -->
		<title>{title}</title>
		<meta name="title" content={title} />
		<meta name="description" content={description} />
		{keywords.length > 0 && <meta name="keywords" content={keywords.join(', ')} />}
		<meta name="author" content="The Free Korean" />
		<meta name="robots" content="index, follow" />
		<meta name="language" content="Vietnamese" />
		<link rel="canonical" href={fullUrl} />
		
		<!-- Open Graph / Facebook -->
		<meta property="og:type" content={type} />
		<meta property="og:url" content={fullUrl} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={fullImageUrl} />
		<meta property="og:site_name" content="The Free Korean" />
		<meta property="og:locale" content="vi_VN" />
		
		<!-- Twitter -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:url" content={fullUrl} />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={fullImageUrl} />
		
		<!-- Google Analytics -->
		<script async src="https://www.googletagmanager.com/gtag/js?id=G-JNJ099MYS7"></script>
		<script>
			window.dataLayer = window.dataLayer || [];
			function gtag(){dataLayer.push(arguments);}
			gtag('js', new Date());
			gtag('config', 'G-JNJ099MYS7');
		</script>
	</head>
	<body>
		<div id="adblock-banner" class="adblock-banner" hidden>
			<span>Trang web không có quảng cáo, nên hy vọng bạn có thể tắt chặn quảng cáo trên trang web để tránh chặn nhầm nội dung quan trọng.</span>
			<button type="button" class="adblock-banner__close" aria-label="Đóng cảnh báo">×</button>
		</div>
		<NavBar />
		<slot />
		<Footer />
		<ScrollToTop />
		<script>
			(function () {
				try {
					const STORAGE_KEY = 'tfk-adblock-banner-dismissed';
					const banner = document.getElementById('adblock-banner');
					if (!banner) {
						return;
					}
					const closeButton = banner.querySelector('.adblock-banner__close');
					closeButton?.addEventListener('click', () => {
						banner.hidden = true;
						try {
							localStorage.setItem(STORAGE_KEY, '1');
						} catch (error) {
							console.warn('Không thể lưu trạng thái banner:', error);
						}
					});
					try {
						if (localStorage.getItem(STORAGE_KEY) === '1') {
							return;
						}
					} catch (error) {
						console.warn('Không thể đọc trạng thái banner:', error);
					}
					const showBanner = () => {
						if (!banner.hidden) {
							return;
						}
						banner.hidden = false;
					};
					const gaScript = document.querySelector('script[src*="googletagmanager.com/gtag/js"]');
					let checkTimer;
					if (gaScript) {
						gaScript.addEventListener('load', () => {
							if (checkTimer) {
								clearTimeout(checkTimer);
							}
						});
						gaScript.addEventListener('error', () => {
							if (checkTimer) {
								clearTimeout(checkTimer);
							}
							showBanner();
						});
						checkTimer = setTimeout(() => {
							showBanner();
						}, 4000);
					} else {
						showBanner();
					}
				} catch (error) {
					console.warn('Không thể khởi tạo banner chặn quảng cáo:', error);
				}
			})();
		</script>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
	
	body {
		padding-top: 70px; /* offset fixed navbar height */
	}

	.adblock-banner {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		display: flex;
		align-items: center;
		gap: 12px;
		background: #ffe08a;
		color: #4a1d1f;
		padding: 12px 16px;
		border-bottom: 1px solid rgba(0, 0, 0, 0.1);
		z-index: 1000;
		font-size: 0.95rem;
		line-height: 1.4;
	}

	.adblock-banner__close {
		margin-left: auto;
		background: transparent;
		border: none;
		font-size: 1.2rem;
		line-height: 1;
		cursor: pointer;
		color: inherit;
		padding: 4px;
	}
</style>
